<?php
/**
 * @var \Docker\API\Model\ImageItem[] $images
 */
?>

<!-- page content -->
<div class="right_col" role="main">
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>Images</h2>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <form action="#" method="post" id="bulk-form">
                        <div class="row">
                            <div class="btn-group">
                                <a class="btn btn-default" onclick="submit('remove')">
                                    <i class="fa fa-trash" aria-hidden="true"></i> Remove
                                </a>
                                <a class="btn btn-danger" onclick="submit('forceRemove')">
                                    <i class="fa fa-trash" aria-hidden="true"></i> Force Remove
                                </a>
                            </div>
                        </div>
                        <table class="table">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Repository</th>
                                <th>Image ID</th>
                                <th>Created</th>
                                <th>Size</th>
                                <th>Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($images as $image) : ?>
                                <?php if ($image->getRepoTags() !== null) : ?>
                                    <?php foreach ($image->getRepoTags() as $tag) : ?>
                                        <tr>
                                            <td>
                                                <?php if ($tag === '<none>:<none>') : ?>
                                                    <input type="checkbox" name="id[]" value="<?= $image->getId() ?>">
                                                <?php else: ?>
                                                    <input type="checkbox" name="id[]" value="<?= $tag ?>">
                                                <?php endif; ?>
                                            </td>
                                            <td><?= htmlspecialchars($tag) ?></td>
                                            <td><?= $image->getId() ?></td>
                                            <td><?= date(DateTime::COOKIE, $image->getCreated()) ?></td>
                                            <td><?= formatBytes($image->getSize()) ?></td>
                                            <td>
                                                <div class="btn-group">
                                                    <?php if ($tag === '<none>:<none>') : ?>
                                                        <a href="/image/remove/<?= $image->getId() ?>"
                                                           class="btn btn-default btn-xs">
                                                            <i class="fa fa-trash" aria-hidden="true"></i>
                                                        </a>
                                                        <a href="/image/forceRemove/<?= $image->getId() ?>"
                                                           class="btn btn-danger btn-xs" title="Force Remove">
                                                            <i class="fa fa-trash" aria-hidden="true"></i>
                                                        </a>
                                                    <?php else: ?>
                                                        <a href="/image/remove/<?= $tag ?>"
                                                           class="btn btn-default btn-xs">
                                                            <i class="fa fa-trash" aria-hidden="true"></i>
                                                        </a>
                                                        <a href="/image/forceRemove/<?= $tag ?>"
                                                           class="btn btn-danger btn-xs" title="Force Remove">
                                                            <i class="fa fa-trash" aria-hidden="true"></i>
                                                        </a>
                                                    <?php endif; ?>
                                                </div>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                <?php endif; ?>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                    </form>
                </div>
            </div>
        </div>

    </div>
</div>
<!-- /page content -->

<script>
    function submit(action) {
        var form = document.getElementById('bulk-form');
        form.setAttribute('action', '/image/bulk/' + action);
        form.submit();
    }
</script>