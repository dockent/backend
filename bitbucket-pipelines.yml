image: mileschou/phalcon

pipelines:
  branches:
    master:
      - step:
          caches:
            - composer
          script:
            - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
            - composer install --ignore-platform-reqs && composer test